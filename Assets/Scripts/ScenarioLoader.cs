using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ShapeReality
{
    /// <summary>
    /// The scenario loader is responsible for loading a specific scenario from the
    /// user interface, each scenario has a specific temperature, how much time it
    /// will 
    /// </summary>
    public class ScenarioLoader : MonoBehaviour
    {
        // The data from the scenario can be exported by this thing?

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}
